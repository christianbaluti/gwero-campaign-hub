import { a as Button, n as CardContent, o as cn, s as AppShell, t as Card } from "./card-CBzG8Uef.js";
import { t as Badge } from "./badge-Ci6XGb5B.js";
import { t as Input } from "./input-B7I4vy8H.js";
import { a as SelectValue, i as SelectTrigger, n as SelectContent, r as SelectItem, t as Select } from "./select-D6Lp5M71.js";
import { t as supabase } from "./client-BCblZ87S.js";
import { t as Label } from "./label-BAtza3LO.js";
import { a as TableHeader, i as TableHead, n as TableBody, o as TableRow, r as TableCell, t as Table } from "./table-PceKd7UZ.js";
import * as React from "react";
import { useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { X } from "lucide-react";
import * as XLSX from "xlsx";
import * as DialogPrimitive from "@radix-ui/react-dialog";
//#region src/components/ui/dialog.tsx
var Dialog = DialogPrimitive.Root;
var DialogTrigger = DialogPrimitive.Trigger;
var DialogPortal = DialogPrimitive.Portal;
var DialogOverlay = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx(DialogPrimitive.Overlay, {
	ref,
	className: cn("fixed inset-0 z-50 bg-black/80  data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0", className),
	...props
}));
DialogOverlay.displayName = DialogPrimitive.Overlay.displayName;
var DialogContent = React.forwardRef(({ className, children, ...props }, ref) => /* @__PURE__ */ jsxs(DialogPortal, { children: [/* @__PURE__ */ jsx(DialogOverlay, {}), /* @__PURE__ */ jsxs(DialogPrimitive.Content, {
	ref,
	className: cn("fixed left-[50%] top-[50%] z-50 grid w-full max-w-lg translate-x-[-50%] translate-y-[-50%] gap-4 border bg-background p-6 shadow-lg duration-200 data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 sm:rounded-lg", className),
	...props,
	children: [children, /* @__PURE__ */ jsxs(DialogPrimitive.Close, {
		className: "absolute right-4 top-4 rounded-sm opacity-70 ring-offset-background cursor-pointer transition-opacity hover:opacity-100 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 disabled:pointer-events-none data-[state=open]:bg-accent data-[state=open]:text-muted-foreground",
		children: [/* @__PURE__ */ jsx(X, { className: "h-4 w-4" }), /* @__PURE__ */ jsx("span", {
			className: "sr-only",
			children: "Close"
		})]
	})]
})] }));
DialogContent.displayName = DialogPrimitive.Content.displayName;
var DialogHeader = ({ className, ...props }) => /* @__PURE__ */ jsx("div", {
	className: cn("flex flex-col space-y-1.5 text-center sm:text-left", className),
	...props
});
DialogHeader.displayName = "DialogHeader";
var DialogFooter = ({ className, ...props }) => /* @__PURE__ */ jsx("div", {
	className: cn("flex flex-col-reverse sm:flex-row sm:justify-end sm:space-x-2", className),
	...props
});
DialogFooter.displayName = "DialogFooter";
var DialogTitle = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx(DialogPrimitive.Title, {
	ref,
	className: cn("text-lg font-semibold leading-none tracking-tight", className),
	...props
}));
DialogTitle.displayName = DialogPrimitive.Title.displayName;
var DialogDescription = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx(DialogPrimitive.Description, {
	ref,
	className: cn("text-sm text-muted-foreground", className),
	...props
}));
DialogDescription.displayName = DialogPrimitive.Description.displayName;
//#endregion
//#region src/routes/prospects.tsx?tsr-split=component
var FIELDS = [
	{
		key: "email",
		label: "Email (required)"
	},
	{
		key: "first_name",
		label: "First name"
	},
	{
		key: "last_name",
		label: "Last name"
	},
	{
		key: "company",
		label: "Company"
	},
	{
		key: "job_title",
		label: "Job title"
	},
	{
		key: "phone",
		label: "Phone"
	}
];
var STATUSES = [
	"new",
	"contacted",
	"replied",
	"interested",
	"not_interested",
	"client"
];
function ImportDialog({ onDone }) {
	const [open, setOpen] = useState(false);
	const [rows, setRows] = useState([]);
	const [headers, setHeaders] = useState([]);
	const [fileName, setFileName] = useState("");
	const [mapping, setMapping] = useState({});
	const [busy, setBusy] = useState(false);
	async function handleFile(file) {
		const buffer = await file.arrayBuffer();
		const book = XLSX.read(buffer);
		const sheetName = book.SheetNames[0];
		if (!sheetName) return;
		const sheet = book.Sheets[sheetName];
		if (!sheet) return;
		const data = XLSX.utils.sheet_to_json(sheet, { defval: "" });
		if (!data.length) {
			toast.error("That file has no rows.");
			return;
		}
		const cols = Object.keys(data[0]);
		setRows(data);
		setHeaders(cols);
		setFileName(file.name);
		const guess = {};
		for (const field of FIELDS) {
			const hit = cols.find((c) => c.toLowerCase().replace(/[^a-z]/g, "") === field.key.replace(/_/g, ""));
			if (hit) guess[field.key] = hit;
		}
		if (!guess["email"]) {
			const hit = cols.find((c) => c.toLowerCase().includes("mail"));
			if (hit) guess["email"] = hit;
		}
		if (!guess["first_name"]) {
			const hit = cols.find((c) => c.toLowerCase().includes("name"));
			if (hit) guess["first_name"] = hit;
		}
		setMapping(guess);
	}
	async function importRows() {
		if (!mapping["email"]) {
			toast.error("Choose which column holds the email address.");
			return;
		}
		setBusy(true);
		const mapped = rows.map((row) => {
			const used = new Set(Object.values(mapping));
			const extra = {};
			for (const [key, value] of Object.entries(row)) if (!used.has(key) && value !== "") extra[key] = value;
			const pick = (field) => {
				const col = mapping[field];
				const value = col ? row[col] : null;
				return value == null || value === "" ? null : String(value).trim();
			};
			const email = pick("email");
			if (!email || !email.includes("@")) return null;
			return {
				email: email.toLowerCase(),
				first_name: pick("first_name"),
				last_name: pick("last_name"),
				company: pick("company"),
				job_title: pick("job_title"),
				phone: pick("phone"),
				extra,
				source_file: fileName
			};
		}).filter(Boolean);
		if (!mapped.length) {
			setBusy(false);
			toast.error("No valid email addresses found in that column.");
			return;
		}
		const { error } = await supabase.from("prospects").upsert(mapped, { onConflict: "email" });
		setBusy(false);
		if (error) {
			toast.error(error.message);
			return;
		}
		toast.success(`${mapped.length} prospects imported.`);
		setOpen(false);
		setRows([]);
		setHeaders([]);
		onDone();
	}
	return /* @__PURE__ */ jsxs(Dialog, {
		open,
		onOpenChange: setOpen,
		children: [/* @__PURE__ */ jsx(DialogTrigger, {
			asChild: true,
			children: /* @__PURE__ */ jsx(Button, { children: "Upload spreadsheet" })
		}), /* @__PURE__ */ jsxs(DialogContent, {
			className: "max-h-[85vh] overflow-y-auto sm:max-w-2xl",
			children: [
				/* @__PURE__ */ jsxs(DialogHeader, { children: [/* @__PURE__ */ jsx(DialogTitle, { children: "Import prospects" }), /* @__PURE__ */ jsx(DialogDescription, { children: "Upload an Excel or CSV file, then tell us which column holds what." })] }),
				/* @__PURE__ */ jsx(Input, {
					type: "file",
					accept: ".xlsx,.xls,.csv",
					onChange: (e) => {
						const file = e.target.files?.[0];
						if (file) handleFile(file);
					}
				}),
				headers.length > 0 ? /* @__PURE__ */ jsxs("div", {
					className: "space-y-4",
					children: [
						/* @__PURE__ */ jsxs("p", {
							className: "text-sm text-muted-foreground",
							children: [
								rows.length,
								" rows found in ",
								fileName,
								". Anything you don't map is kept as extra details you can still use as placeholders."
							]
						}),
						/* @__PURE__ */ jsx("div", {
							className: "grid gap-3 sm:grid-cols-2",
							children: FIELDS.map((field) => /* @__PURE__ */ jsxs("div", {
								className: "space-y-1",
								children: [/* @__PURE__ */ jsx(Label, { children: field.label }), /* @__PURE__ */ jsxs(Select, {
									value: mapping[field.key] ?? "__none",
									onValueChange: (v) => setMapping((m) => {
										const next = { ...m };
										if (v === "__none") delete next[field.key];
										else next[field.key] = v;
										return next;
									}),
									children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, { placeholder: "Not used" }) }), /* @__PURE__ */ jsxs(SelectContent, { children: [/* @__PURE__ */ jsx(SelectItem, {
										value: "__none",
										children: "Not used"
									}), headers.map((h) => /* @__PURE__ */ jsx(SelectItem, {
										value: h,
										children: h
									}, h))] })]
								})]
							}, field.key))
						}),
						/* @__PURE__ */ jsx(Button, {
							onClick: () => void importRows(),
							disabled: busy,
							children: busy ? "Importing…" : `Import ${rows.length} rows`
						})
					]
				}) : null
			]
		})]
	});
}
function ProspectsPage() {
	const qc = useQueryClient();
	const [search, setSearch] = useState("");
	const { data: prospects = [] } = useQuery({
		queryKey: ["prospects"],
		queryFn: async () => {
			const { data, error } = await supabase.from("prospects").select("*").order("created_at", { ascending: false });
			if (error) throw error;
			return data;
		}
	});
	const setStatus = useMutation({
		mutationFn: async ({ id, status }) => {
			const { error } = await supabase.from("prospects").update({ status }).eq("id", id);
			if (error) throw error;
		},
		onSuccess: () => qc.invalidateQueries({ queryKey: ["prospects"] })
	});
	const convert = useMutation({
		mutationFn: async (prospect) => {
			const { error } = await supabase.from("clients").insert({
				name: [prospect.first_name, prospect.last_name].filter(Boolean).join(" ") || prospect.email,
				company: prospect.company,
				email: prospect.email,
				phone: prospect.phone,
				prospect_id: prospect.id
			});
			if (error) throw error;
			await supabase.from("prospects").update({ status: "client" }).eq("id", prospect.id);
		},
		onSuccess: () => {
			toast.success("Prospect converted to a client.");
			qc.invalidateQueries({ queryKey: ["prospects"] });
			qc.invalidateQueries({ queryKey: ["clients"] });
		},
		onError: (e) => toast.error(e.message)
	});
	const remove = useMutation({
		mutationFn: async (id) => {
			const { error } = await supabase.from("prospects").delete().eq("id", id);
			if (error) throw error;
		},
		onSuccess: () => qc.invalidateQueries({ queryKey: ["prospects"] })
	});
	const filtered = prospects.filter((p) => `${p.email} ${p.first_name ?? ""} ${p.last_name ?? ""} ${p.company ?? ""}`.toLowerCase().includes(search.toLowerCase()));
	return /* @__PURE__ */ jsx(AppShell, {
		title: "Prospects",
		description: "Everyone you might sell to, imported from your spreadsheets.",
		actions: /* @__PURE__ */ jsx(ImportDialog, { onDone: () => qc.invalidateQueries({ queryKey: ["prospects"] }) }),
		children: /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsxs(CardContent, {
			className: "pt-6",
			children: [
				/* @__PURE__ */ jsx(Input, {
					placeholder: "Search by name, email or company",
					value: search,
					onChange: (e) => setSearch(e.target.value),
					className: "mb-4 max-w-sm"
				}),
				/* @__PURE__ */ jsx("div", {
					className: "overflow-x-auto",
					children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
						/* @__PURE__ */ jsx(TableHead, { children: "Name" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Email" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Company" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Status" }),
						/* @__PURE__ */ jsx(TableHead, {
							className: "text-right",
							children: "Actions"
						})
					] }) }), /* @__PURE__ */ jsxs(TableBody, { children: [filtered.map((p) => /* @__PURE__ */ jsxs(TableRow, { children: [
						/* @__PURE__ */ jsx(TableCell, {
							className: "font-medium",
							children: [p.first_name, p.last_name].filter(Boolean).join(" ") || "—"
						}),
						/* @__PURE__ */ jsx(TableCell, { children: p.email }),
						/* @__PURE__ */ jsx(TableCell, { children: p.company ?? "—" }),
						/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsxs(Select, {
							value: p.status,
							onValueChange: (status) => setStatus.mutate({
								id: p.id,
								status
							}),
							children: [/* @__PURE__ */ jsx(SelectTrigger, {
								className: "h-8 w-40",
								children: /* @__PURE__ */ jsx(SelectValue, {})
							}), /* @__PURE__ */ jsx(SelectContent, { children: STATUSES.map((s) => /* @__PURE__ */ jsx(SelectItem, {
								value: s,
								children: s.replace("_", " ")
							}, s)) })]
						}) }),
						/* @__PURE__ */ jsxs(TableCell, {
							className: "space-x-2 text-right",
							children: [/* @__PURE__ */ jsx(Button, {
								size: "sm",
								variant: "outline",
								onClick: () => convert.mutate(p),
								children: "Make client"
							}), /* @__PURE__ */ jsx(Button, {
								size: "sm",
								variant: "ghost",
								onClick: () => remove.mutate(p.id),
								children: "Delete"
							})]
						})
					] }, p.id)), filtered.length === 0 ? /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
						colSpan: 5,
						className: "py-10 text-center text-muted-foreground",
						children: "No prospects yet — upload a spreadsheet to get started."
					}) }) : null] })] })
				}),
				/* @__PURE__ */ jsxs("p", {
					className: "mt-4 text-xs text-muted-foreground",
					children: [/* @__PURE__ */ jsx(Badge, {
						variant: "secondary",
						children: prospects.length
					}), " prospects in total"]
				})
			]
		}) })
	});
}
//#endregion
export { ProspectsPage as component };
