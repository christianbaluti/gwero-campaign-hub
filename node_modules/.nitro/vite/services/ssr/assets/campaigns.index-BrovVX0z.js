import { a as Button, n as CardContent, s as AppShell, t as Card } from "./card-CBzG8Uef.js";
import { t as Badge } from "./badge-Ci6XGb5B.js";
import { t as Input } from "./input-B7I4vy8H.js";
import { t as supabase } from "./client-BCblZ87S.js";
import { a as TableHeader, i as TableHead, n as TableBody, o as TableRow, r as TableCell, t as Table } from "./table-PceKd7UZ.js";
import { useState } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
//#region src/routes/campaigns.index.tsx?tsr-split=component
function CampaignsPage() {
	const qc = useQueryClient();
	const [name, setName] = useState("");
	const { data: campaigns = [] } = useQuery({
		queryKey: ["campaigns"],
		queryFn: async () => {
			const { data, error } = await supabase.from("campaigns").select("*, campaign_recipients(status)").order("created_at", { ascending: false });
			if (error) throw error;
			return data;
		}
	});
	const create = useMutation({
		mutationFn: async () => {
			const { data, error } = await supabase.from("campaigns").insert({
				name: name || "Untitled campaign",
				subject: "",
				body_html: ""
			}).select().single();
			if (error) throw error;
			return data;
		},
		onSuccess: () => {
			setName("");
			toast.success("Campaign created.");
			qc.invalidateQueries({ queryKey: ["campaigns"] });
		},
		onError: (e) => toast.error(e.message)
	});
	return /* @__PURE__ */ jsxs(AppShell, {
		title: "Campaigns",
		description: "Mass emails to your prospect lists.",
		children: [/* @__PURE__ */ jsx(Card, {
			className: "mb-6",
			children: /* @__PURE__ */ jsxs(CardContent, {
				className: "flex flex-wrap items-center gap-3 pt-6",
				children: [/* @__PURE__ */ jsx(Input, {
					className: "max-w-sm",
					placeholder: "New campaign name",
					value: name,
					onChange: (e) => setName(e.target.value)
				}), /* @__PURE__ */ jsx(Button, {
					onClick: () => create.mutate(),
					children: "Create campaign"
				})]
			})
		}), /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsx(CardContent, {
			className: "overflow-x-auto pt-6",
			children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
				/* @__PURE__ */ jsx(TableHead, { children: "Name" }),
				/* @__PURE__ */ jsx(TableHead, { children: "Subject" }),
				/* @__PURE__ */ jsx(TableHead, { children: "Status" }),
				/* @__PURE__ */ jsx(TableHead, { children: "Recipients" }),
				/* @__PURE__ */ jsx(TableHead, { children: "Sent" })
			] }) }), /* @__PURE__ */ jsxs(TableBody, { children: [campaigns.map((c) => {
				const recipients = c.campaign_recipients ?? [];
				return /* @__PURE__ */ jsxs(TableRow, { children: [
					/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsx(Link, {
						to: "/campaigns/$id",
						params: { id: c.id },
						className: "font-medium text-primary hover:underline",
						children: c.name
					}) }),
					/* @__PURE__ */ jsx(TableCell, {
						className: "max-w-xs truncate",
						children: c.subject || "—"
					}),
					/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsx(Badge, {
						variant: c.status === "sent" ? "default" : "secondary",
						children: c.status
					}) }),
					/* @__PURE__ */ jsx(TableCell, { children: recipients.length }),
					/* @__PURE__ */ jsx(TableCell, { children: recipients.filter((r) => r.status === "sent").length })
				] }, c.id);
			}), campaigns.length === 0 ? /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
				colSpan: 5,
				className: "py-10 text-center text-muted-foreground",
				children: "No campaigns yet."
			}) }) : null] })] })
		}) })]
	});
}
//#endregion
export { CampaignsPage as component };
