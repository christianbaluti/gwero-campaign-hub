import { a as Button, c as money, n as CardContent, s as AppShell, t as Card } from "./card-CBzG8Uef.js";
import { t as Badge } from "./badge-Ci6XGb5B.js";
import { t as Input } from "./input-B7I4vy8H.js";
import { a as SelectValue, i as SelectTrigger, n as SelectContent, r as SelectItem, t as Select } from "./select-D6Lp5M71.js";
import { t as supabase } from "./client-BCblZ87S.js";
import { useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
//#region src/routes/agreements.tsx?tsr-split=component
function AgreementsPage() {
	const qc = useQueryClient();
	const [title, setTitle] = useState("");
	const [clientId, setClientId] = useState("");
	const { data: clients = [] } = useQuery({
		queryKey: ["clients"],
		queryFn: async () => {
			const { data, error } = await supabase.from("clients").select("id,name").order("name");
			if (error) throw error;
			return data;
		}
	});
	const { data: agreements = [] } = useQuery({
		queryKey: ["agreements"],
		queryFn: async () => {
			const { data, error } = await supabase.from("agreements").select("*, clients(name)").order("created_at", { ascending: false });
			if (error) throw error;
			return data;
		}
	});
	const create = useMutation({
		mutationFn: async () => {
			if (!title.trim() || !clientId) throw new Error("Choose a client and enter a title.");
			const { error } = await supabase.from("agreements").insert({
				title: title.trim(),
				client_id: clientId
			});
			if (error) throw error;
		},
		onSuccess: () => {
			setTitle("");
			toast.success("Agreement added.");
			qc.invalidateQueries({ queryKey: ["agreements"] });
		},
		onError: (e) => toast.error(e.message)
	});
	return /* @__PURE__ */ jsxs(AppShell, {
		title: "Agreements & SLAs",
		description: "Track contracts, service commitments and renewals.",
		children: [/* @__PURE__ */ jsx(Card, {
			className: "mb-6",
			children: /* @__PURE__ */ jsxs(CardContent, {
				className: "flex flex-wrap gap-3 pt-6",
				children: [
					/* @__PURE__ */ jsx(Input, {
						className: "max-w-xs",
						placeholder: "Agreement title",
						value: title,
						onChange: (e) => setTitle(e.target.value)
					}),
					/* @__PURE__ */ jsxs(Select, {
						value: clientId,
						onValueChange: setClientId,
						children: [/* @__PURE__ */ jsx(SelectTrigger, {
							className: "max-w-xs",
							children: /* @__PURE__ */ jsx(SelectValue, { placeholder: "Choose client" })
						}), /* @__PURE__ */ jsx(SelectContent, { children: clients.map((c) => /* @__PURE__ */ jsx(SelectItem, {
							value: c.id,
							children: c.name
						}, c.id)) })]
					}),
					/* @__PURE__ */ jsx(Button, {
						onClick: () => create.mutate(),
						children: "Add agreement"
					})
				]
			})
		}), /* @__PURE__ */ jsxs("div", {
			className: "grid gap-4 md:grid-cols-2 xl:grid-cols-3",
			children: [agreements.map((a) => /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsxs(CardContent, {
				className: "pt-6",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "flex justify-between gap-3",
					children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("h2", {
						className: "font-semibold",
						children: a.title
					}), /* @__PURE__ */ jsx("p", {
						className: "mt-1 text-sm text-muted-foreground",
						children: a.clients?.name
					})] }), /* @__PURE__ */ jsx(Badge, {
						variant: "secondary",
						children: a.status
					})]
				}), /* @__PURE__ */ jsxs("dl", {
					className: "mt-5 grid grid-cols-2 gap-3 text-sm",
					children: [
						/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("dt", {
							className: "text-muted-foreground",
							children: "Type"
						}), /* @__PURE__ */ jsx("dd", {
							className: "capitalize",
							children: a.agreement_type
						})] }),
						/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("dt", {
							className: "text-muted-foreground",
							children: "Value"
						}), /* @__PURE__ */ jsx("dd", { children: money(a.value, a.currency) })] }),
						/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("dt", {
							className: "text-muted-foreground",
							children: "Response"
						}), /* @__PURE__ */ jsx("dd", { children: a.response_time_hours ? `${a.response_time_hours} hours` : "Not set" })] }),
						/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("dt", {
							className: "text-muted-foreground",
							children: "Renews"
						}), /* @__PURE__ */ jsx("dd", { children: a.auto_renew ? "Automatically" : a.end_date || "Not set" })] })
					]
				})]
			}) }, a.id)), agreements.length === 0 ? /* @__PURE__ */ jsx("p", {
				className: "text-sm text-muted-foreground",
				children: "No agreements yet."
			}) : null]
		})]
	});
}
//#endregion
export { AgreementsPage as component };
