import { a as useServerFn, n as sendCampaign } from "./crm.functions-CpcA28n0.js";
import { t as BASE_PLACEHOLDERS } from "./personalize-BPwaxYCo.js";
import { t as Route } from "./campaigns._id-MdvkoFfi.js";
import { a as Button, i as CardTitle, n as CardContent, o as cn, r as CardHeader, s as AppShell, t as Card } from "./card-CBzG8Uef.js";
import { t as Badge } from "./badge-Ci6XGb5B.js";
import { t as Input } from "./input-B7I4vy8H.js";
import { a as SelectValue, i as SelectTrigger, n as SelectContent, r as SelectItem, t as Select } from "./select-D6Lp5M71.js";
import { t as supabase } from "./client-BCblZ87S.js";
import { t as Label } from "./label-BAtza3LO.js";
import { t as Textarea } from "./textarea-tHeXRFzh.js";
import { a as TableHeader, i as TableHead, n as TableBody, o as TableRow, r as TableCell, t as Table } from "./table-PceKd7UZ.js";
import * as React from "react";
import { useEffect, useState } from "react";
import { Link } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import * as SwitchPrimitives from "@radix-ui/react-switch";
import * as TabsPrimitive from "@radix-ui/react-tabs";
//#region src/components/ui/switch.tsx
var Switch = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx(SwitchPrimitives.Root, {
	className: cn("peer inline-flex h-5 w-9 shrink-0 cursor-pointer items-center rounded-full border-2 border-transparent shadow-sm transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background disabled:cursor-not-allowed disabled:opacity-50 data-[state=checked]:bg-primary data-[state=unchecked]:bg-input", className),
	...props,
	ref,
	children: /* @__PURE__ */ jsx(SwitchPrimitives.Thumb, { className: cn("pointer-events-none block h-4 w-4 rounded-full bg-background shadow-lg ring-0 transition-transform data-[state=checked]:translate-x-4 data-[state=unchecked]:translate-x-0") })
}));
Switch.displayName = SwitchPrimitives.Root.displayName;
//#endregion
//#region src/components/ui/tabs.tsx
var Tabs = TabsPrimitive.Root;
var TabsList = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx(TabsPrimitive.List, {
	ref,
	className: cn("inline-flex h-9 items-center justify-center rounded-lg bg-muted p-1 text-muted-foreground", className),
	...props
}));
TabsList.displayName = TabsPrimitive.List.displayName;
var TabsTrigger = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx(TabsPrimitive.Trigger, {
	ref,
	className: cn("inline-flex items-center justify-center whitespace-nowrap rounded-md px-3 py-1 text-sm font-medium ring-offset-background cursor-pointer transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 disabled:cursor-not-allowed data-[state=active]:bg-background data-[state=active]:text-foreground data-[state=active]:shadow", className),
	...props
}));
TabsTrigger.displayName = TabsPrimitive.Trigger.displayName;
var TabsContent = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx(TabsPrimitive.Content, {
	ref,
	className: cn("mt-2 ring-offset-background focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2", className),
	...props
}));
TabsContent.displayName = TabsPrimitive.Content.displayName;
//#endregion
//#region src/routes/campaigns.$id.tsx?tsr-split=component
function CampaignDetail() {
	const { id } = Route.useParams();
	const qc = useQueryClient();
	const send = useServerFn(sendCampaign);
	const { data: campaign } = useQuery({
		queryKey: ["campaign", id],
		queryFn: async () => {
			const { data, error } = await supabase.from("campaigns").select("*").eq("id", id).single();
			if (error) throw error;
			return data;
		}
	});
	const { data: mailboxes = [] } = useQuery({
		queryKey: ["mailboxes"],
		queryFn: async () => {
			const { data, error } = await supabase.from("mailboxes").select("*").order("name");
			if (error) throw error;
			return data;
		}
	});
	const { data: prospects = [] } = useQuery({
		queryKey: ["prospects"],
		queryFn: async () => {
			const { data, error } = await supabase.from("prospects").select("*").order("created_at", { ascending: false });
			if (error) throw error;
			return data;
		}
	});
	const { data: recipients = [] } = useQuery({
		queryKey: ["recipients", id],
		queryFn: async () => {
			const { data, error } = await supabase.from("campaign_recipients").select("*, prospects(email, first_name, last_name, company)").eq("campaign_id", id);
			if (error) throw error;
			return data;
		},
		refetchInterval: 5e3
	});
	const [form, setForm] = useState({
		name: "",
		subject: "",
		body_html: "",
		cc: "",
		bcc: "",
		mailbox_id: "",
		track_opens: true,
		track_clicks: true
	});
	const [attachments, setAttachments] = useState([]);
	const [sending, setSending] = useState(false);
	useEffect(() => {
		if (!campaign) return;
		setForm({
			name: campaign.name,
			subject: campaign.subject,
			body_html: campaign.body_html,
			cc: (campaign.cc ?? []).join(", "),
			bcc: (campaign.bcc ?? []).join(", "),
			mailbox_id: campaign.mailbox_id ?? "",
			track_opens: campaign.track_opens,
			track_clicks: campaign.track_clicks
		});
		setAttachments(campaign.attachments ?? []);
	}, [campaign]);
	const save = useMutation({
		mutationFn: async (extra) => {
			const list = extra?.attachments ?? attachments;
			const { error } = await supabase.from("campaigns").update({
				name: form.name,
				subject: form.subject,
				body_html: form.body_html,
				cc: form.cc.split(",").map((s) => s.trim()).filter(Boolean),
				bcc: form.bcc.split(",").map((s) => s.trim()).filter(Boolean),
				mailbox_id: form.mailbox_id || null,
				track_opens: form.track_opens,
				track_clicks: form.track_clicks,
				attachments: list
			}).eq("id", id);
			if (error) throw error;
		},
		onSuccess: () => {
			toast.success("Saved.");
			qc.invalidateQueries({ queryKey: ["campaign", id] });
		},
		onError: (e) => toast.error(e.message)
	});
	async function uploadFile(file) {
		const path = `${id}/${Date.now()}-${file.name}`;
		const { error } = await supabase.storage.from("attachments").upload(path, file);
		if (error) {
			toast.error(error.message);
			return;
		}
		const next = [...attachments, {
			path,
			name: file.name,
			type: file.type,
			size: file.size
		}];
		setAttachments(next);
		save.mutate({ attachments: next });
	}
	const addRecipients = useMutation({
		mutationFn: async (status) => {
			const pool = status === "all" ? prospects : prospects.filter((p) => p.status === status);
			const existing = new Set(recipients.map((r) => r.prospect_id));
			const rows = pool.filter((p) => !existing.has(p.id)).map((p) => ({
				campaign_id: id,
				prospect_id: p.id
			}));
			if (!rows.length) throw new Error("No new prospects to add.");
			const { error } = await supabase.from("campaign_recipients").insert(rows);
			if (error) throw error;
			return rows.length;
		},
		onSuccess: (count) => {
			toast.success(`${count} recipients added.`);
			qc.invalidateQueries({ queryKey: ["recipients", id] });
		},
		onError: (e) => toast.error(e.message)
	});
	async function handleSend() {
		setSending(true);
		try {
			await save.mutateAsync(void 0);
			const result = await send({ data: { campaignId: id } });
			toast.success(`Sent ${result.sent}, failed ${result.failed}.`);
			qc.invalidateQueries({ queryKey: ["recipients", id] });
			qc.invalidateQueries({ queryKey: ["campaign", id] });
		} catch (error) {
			toast.error(error.message);
		} finally {
			setSending(false);
		}
	}
	const stats = {
		total: recipients.length,
		sent: recipients.filter((r) => r.status === "sent").length,
		failed: recipients.filter((r) => r.status === "failed").length,
		opened: recipients.filter((r) => (r.open_count ?? 0) > 0).length,
		clicked: recipients.filter((r) => (r.click_count ?? 0) > 0).length,
		replied: recipients.filter((r) => r.replied_at).length
	};
	const extraKeys = Array.from(new Set(prospects.flatMap((p) => Object.keys(p.extra ?? {})))).slice(0, 20);
	return /* @__PURE__ */ jsx(AppShell, {
		title: campaign?.name ?? "Campaign",
		description: "Write once, personalise per prospect, and send.",
		actions: /* @__PURE__ */ jsxs(Fragment, { children: [
			/* @__PURE__ */ jsx(Button, {
				variant: "outline",
				asChild: true,
				children: /* @__PURE__ */ jsx(Link, {
					to: "/campaigns",
					children: "Back"
				})
			}),
			/* @__PURE__ */ jsx(Button, {
				variant: "outline",
				onClick: () => save.mutate(void 0),
				children: "Save"
			}),
			/* @__PURE__ */ jsx(Button, {
				onClick: () => void handleSend(),
				disabled: sending,
				children: sending ? "Sending…" : "Send campaign"
			})
		] }),
		children: /* @__PURE__ */ jsxs(Tabs, {
			defaultValue: "compose",
			children: [
				/* @__PURE__ */ jsxs(TabsList, { children: [
					/* @__PURE__ */ jsx(TabsTrigger, {
						value: "compose",
						children: "Compose"
					}),
					/* @__PURE__ */ jsxs(TabsTrigger, {
						value: "recipients",
						children: [
							"Recipients (",
							stats.total,
							")"
						]
					}),
					/* @__PURE__ */ jsx(TabsTrigger, {
						value: "results",
						children: "Results"
					})
				] }),
				/* @__PURE__ */ jsxs(TabsContent, {
					value: "compose",
					className: "mt-4 grid gap-6 lg:grid-cols-[2fr_1fr]",
					children: [/* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsxs(CardContent, {
						className: "space-y-4 pt-6",
						children: [
							/* @__PURE__ */ jsxs("div", {
								className: "space-y-1",
								children: [/* @__PURE__ */ jsx(Label, { children: "Campaign name" }), /* @__PURE__ */ jsx(Input, {
									value: form.name,
									onChange: (e) => setForm({
										...form,
										name: e.target.value
									})
								})]
							}),
							/* @__PURE__ */ jsxs("div", {
								className: "space-y-1",
								children: [/* @__PURE__ */ jsx(Label, { children: "Subject" }), /* @__PURE__ */ jsx(Input, {
									value: form.subject,
									onChange: (e) => setForm({
										...form,
										subject: e.target.value
									}),
									placeholder: "Hi {{first_name}}, a quick idea for {{company}}"
								})]
							}),
							/* @__PURE__ */ jsxs("div", {
								className: "space-y-1",
								children: [/* @__PURE__ */ jsx(Label, { children: "Message (HTML allowed)" }), /* @__PURE__ */ jsx(Textarea, {
									rows: 16,
									value: form.body_html,
									onChange: (e) => setForm({
										...form,
										body_html: e.target.value
									}),
									placeholder: "<p>Hi {{first_name}},</p>"
								})]
							}),
							/* @__PURE__ */ jsxs("div", {
								className: "grid gap-3 sm:grid-cols-2",
								children: [/* @__PURE__ */ jsxs("div", {
									className: "space-y-1",
									children: [/* @__PURE__ */ jsx(Label, { children: "CC (comma separated)" }), /* @__PURE__ */ jsx(Input, {
										value: form.cc,
										onChange: (e) => setForm({
											...form,
											cc: e.target.value
										})
									})]
								}), /* @__PURE__ */ jsxs("div", {
									className: "space-y-1",
									children: [/* @__PURE__ */ jsx(Label, { children: "BCC (comma separated)" }), /* @__PURE__ */ jsx(Input, {
										value: form.bcc,
										onChange: (e) => setForm({
											...form,
											bcc: e.target.value
										})
									})]
								})]
							})
						]
					}) }), /* @__PURE__ */ jsxs("div", {
						className: "space-y-6",
						children: [
							/* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, {
								className: "text-base",
								children: "Sending account"
							}) }), /* @__PURE__ */ jsxs(CardContent, {
								className: "space-y-4",
								children: [
									/* @__PURE__ */ jsxs(Select, {
										value: form.mailbox_id || "__none",
										onValueChange: (v) => setForm({
											...form,
											mailbox_id: v === "__none" ? "" : v
										}),
										children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, { placeholder: "Pick an account" }) }), /* @__PURE__ */ jsxs(SelectContent, { children: [/* @__PURE__ */ jsx(SelectItem, {
											value: "__none",
											children: "Not chosen"
										}), mailboxes.map((m) => /* @__PURE__ */ jsxs(SelectItem, {
											value: m.id,
											children: [
												m.name,
												" · ",
												m.from_email
											]
										}, m.id))] })]
									}),
									/* @__PURE__ */ jsxs("div", {
										className: "flex items-center justify-between",
										children: [/* @__PURE__ */ jsx(Label, { children: "Track opens" }), /* @__PURE__ */ jsx(Switch, {
											checked: form.track_opens,
											onCheckedChange: (v) => setForm({
												...form,
												track_opens: v
											})
										})]
									}),
									/* @__PURE__ */ jsxs("div", {
										className: "flex items-center justify-between",
										children: [/* @__PURE__ */ jsx(Label, { children: "Track link clicks" }), /* @__PURE__ */ jsx(Switch, {
											checked: form.track_clicks,
											onCheckedChange: (v) => setForm({
												...form,
												track_clicks: v
											})
										})]
									})
								]
							})] }),
							/* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, {
								className: "text-base",
								children: "Attachments"
							}) }), /* @__PURE__ */ jsxs(CardContent, {
								className: "space-y-3",
								children: [/* @__PURE__ */ jsx(Input, {
									type: "file",
									onChange: (e) => {
										const file = e.target.files?.[0];
										if (file) uploadFile(file);
									}
								}), attachments.map((a) => /* @__PURE__ */ jsxs("div", {
									className: "flex items-center justify-between text-sm",
									children: [/* @__PURE__ */ jsx("span", {
										className: "truncate",
										children: a.name
									}), /* @__PURE__ */ jsx(Button, {
										size: "sm",
										variant: "ghost",
										onClick: () => {
											const next = attachments.filter((x) => x.path !== a.path);
											setAttachments(next);
											save.mutate({ attachments: next });
										},
										children: "Remove"
									})]
								}, a.path))]
							})] }),
							/* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, {
								className: "text-base",
								children: "Placeholders"
							}) }), /* @__PURE__ */ jsx(CardContent, {
								className: "flex flex-wrap gap-2",
								children: [...BASE_PLACEHOLDERS, ...extraKeys].map((key) => /* @__PURE__ */ jsx(Badge, {
									variant: "secondary",
									children: `{{${key}}}`
								}, key))
							})] })
						]
					})]
				}),
				/* @__PURE__ */ jsxs(TabsContent, {
					value: "recipients",
					className: "mt-4 space-y-4",
					children: [/* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsx(CardContent, {
						className: "flex flex-wrap gap-2 pt-6",
						children: [
							"all",
							"new",
							"contacted",
							"interested"
						].map((status) => /* @__PURE__ */ jsxs(Button, {
							variant: "outline",
							onClick: () => addRecipients.mutate(status),
							children: [
								"Add ",
								status,
								" prospects"
							]
						}, status))
					}) }), /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsx(CardContent, {
						className: "overflow-x-auto pt-6",
						children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
							/* @__PURE__ */ jsx(TableHead, { children: "Prospect" }),
							/* @__PURE__ */ jsx(TableHead, { children: "Email" }),
							/* @__PURE__ */ jsx(TableHead, { children: "Status" }),
							/* @__PURE__ */ jsx(TableHead, { className: "text-right" })
						] }) }), /* @__PURE__ */ jsx(TableBody, { children: recipients.map((r) => {
							const p = r.prospects;
							return /* @__PURE__ */ jsxs(TableRow, { children: [
								/* @__PURE__ */ jsx(TableCell, { children: [p?.first_name, p?.last_name].filter(Boolean).join(" ") || "—" }),
								/* @__PURE__ */ jsx(TableCell, { children: p?.email }),
								/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsx(Badge, {
									variant: r.status === "failed" ? "destructive" : "secondary",
									children: r.status
								}) }),
								/* @__PURE__ */ jsx(TableCell, {
									className: "text-right",
									children: /* @__PURE__ */ jsx(Button, {
										size: "sm",
										variant: "ghost",
										onClick: async () => {
											await supabase.from("campaign_recipients").delete().eq("id", r.id);
											qc.invalidateQueries({ queryKey: ["recipients", id] });
										},
										children: "Remove"
									})
								})
							] }, r.id);
						}) })] })
					}) })]
				}),
				/* @__PURE__ */ jsxs(TabsContent, {
					value: "results",
					className: "mt-4 space-y-4",
					children: [/* @__PURE__ */ jsx("div", {
						className: "grid gap-4 sm:grid-cols-3 lg:grid-cols-6",
						children: Object.entries(stats).map(([key, value]) => /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsxs(CardContent, {
							className: "pt-6",
							children: [/* @__PURE__ */ jsx("p", {
								className: "text-xs uppercase text-muted-foreground",
								children: key
							}), /* @__PURE__ */ jsx("p", {
								className: "font-display text-2xl font-semibold",
								children: value
							})]
						}) }, key))
					}), /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsx(CardContent, {
						className: "overflow-x-auto pt-6",
						children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
							/* @__PURE__ */ jsx(TableHead, { children: "Email" }),
							/* @__PURE__ */ jsx(TableHead, { children: "Status" }),
							/* @__PURE__ */ jsx(TableHead, { children: "Opens" }),
							/* @__PURE__ */ jsx(TableHead, { children: "Clicks" }),
							/* @__PURE__ */ jsx(TableHead, { children: "Replied" }),
							/* @__PURE__ */ jsx(TableHead, { children: "Problem" })
						] }) }), /* @__PURE__ */ jsx(TableBody, { children: recipients.map((r) => {
							const p = r.prospects;
							return /* @__PURE__ */ jsxs(TableRow, { children: [
								/* @__PURE__ */ jsx(TableCell, { children: p?.email }),
								/* @__PURE__ */ jsx(TableCell, { children: r.status }),
								/* @__PURE__ */ jsx(TableCell, { children: r.open_count }),
								/* @__PURE__ */ jsx(TableCell, { children: r.click_count }),
								/* @__PURE__ */ jsx(TableCell, { children: r.replied_at ? "Yes" : "—" }),
								/* @__PURE__ */ jsx(TableCell, {
									className: "max-w-xs truncate text-destructive",
									children: r.error ?? ""
								})
							] }, r.id);
						}) })] })
					}) })]
				})
			]
		})
	});
}
//#endregion
export { CampaignDetail as component };
